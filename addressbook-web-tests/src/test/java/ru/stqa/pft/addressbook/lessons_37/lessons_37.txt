+++ Проверка и обеспечения выполнения предусловий +++
- Дорабатываем методы тестов группы Group, а именно (модификация группы и удаление группы), добавляем условия для проверки наличия групп и если их нет то создаем предварительно а после проводим тесты модификации и у даления. В противном случае при отсутствии условия тесты падают..
Добавили методы:
    //метод, который включает все шаги по созданию группы
    public void createGroup(GroupData group) {}
    //метод для проверки возможности выбрать какую-либо группу
    public boolean isThereAGroup() {}
    //Проверка наличие группы, в случае отсутствия создаем группу.
    ***
    if(!app.getGroupHelper().isThereAGroup()){
          app.getGroupHelper().createGroup(new GroupData("test4", null, "test4"));
    }
    ***

===
Также подобное проделываем для контактов.
